@model IEnumerable<MVCProject.Models.User>
@*@model MVCProject.LecturerViewModel.RegisterationViewModel*@
@*@{
        Layout = null;
    }*@
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Show users</title>
</head>
<body>
    @{
        var courseId = 0;
        if (TempData["CurrentCourseID"] != null)
        {
            courseId = int.Parse(TempData["CurrentCourseID"].ToString());
        }
    }
    <table class="table">
        <tr>
            <th>
                ID
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>

            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a href="/Lecturer/Edit?courseID=@item.Id">@item.Id</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { studentid = item.Id,courseid = courseId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>

            </tr>
        }
    </table>
</body>
</html>
